# This package is published to, and resolves from, the public npm registry.
# Pinned here so a developer's own registry mirror does not end up in package-lock.json.
registry=https://registry.npmjs.org/
